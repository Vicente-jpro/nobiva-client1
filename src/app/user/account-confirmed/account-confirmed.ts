import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-account-confirmed',
  imports: [RouterLink],
  templateUrl: './account-confirmed.html',
})
export class AccountConfirmed {}
